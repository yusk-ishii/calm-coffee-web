<button id="js-menu-button" class="menu-button">
  <span class="line"></span>
  <span class="line"></span>
</button>

<style lang="scss" is:global>
  .menu-button {
    position: relative;
    z-index: 500;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: clamp-size(46);
    height: clamp-size(46);
    pointer-events: auto;
    background-color: var(--color-main);
    border-radius: 999px;

    .open & {
      background-color: transparent;
    }
  }

  .line {
    --line-position: 3;

    position: absolute;
    width: clamp-size(20);
    height: 2px;
    background-color: #fff;
    border-radius: 999px;
    transform-origin: center;

    &:nth-of-type(1) {
      transform: translateY(calc(clamp-size(var(--line-position)) * -1));
    }

    &:nth-of-type(2) {
      transform: translateY(clamp-size(var(--line-position)));
    }

    .open & {
      &:nth-of-type(1) {
        animation: open-top-line 0.3s var(--ease-out-sine) forwards;
      }

      &:nth-of-type(2) {
        animation: open-bottom-line 0.3s var(--ease-out-sine) forwards;
      }
    }

    .close & {
      &:nth-of-type(1) {
        animation: close-top-line 0.3s var(--ease-out-sine) forwards;
      }

      &:nth-of-type(2) {
        animation: close-bottom-line 0.3s var(--ease-out-sine) forwards;
      }
    }
  }

  @keyframes open-top-line {
    0% {
      transform: translateY(calc(clamp-size(var(--line-position)) * -1)) rotate(0);
    }

    50% {
      transform: translateY(0) rotate(0);
    }

    100% {
      transform: translateY(0) rotate(30deg);
    }
  }

  @keyframes open-bottom-line {
    0% {
      transform: translateY(clamp-size(var(--line-position))) rotate(0);
    }

    50% {
      transform: translateY(0) rotate(0);
    }

    100% {
      transform: translateY(0) rotate(-30deg);
    }
  }

  @keyframes close-top-line {
    0% {
      transform: translateY(0) rotate(30deg);
    }

    50% {
      transform: translateY(0) rotate(0);
    }

    100% {
      transform: translateY(calc(clamp-size(var(--line-position)) * -1)) rotate(0);
    }
  }

  @keyframes close-bottom-line {
    0% {
      transform: translateY(0) rotate(-30deg);
    }

    50% {
      transform: translateY(0) rotate(0);
    }

    100% {
      transform: translateY(clamp-size(var(--line-position))) rotate(0);
    }
  }
</style>
